# 배포 방법 — 앙상블 메이

## 왜 지금까지 안 됐나

Netlify 는 **끌어다 놓은 폴더의 맨 위에 `index.html` 이 있는지** 만 봅니다.
`E:\ensemblemay` 뿌리에는 `index.html` 이 없습니다. 여기에 있는 건

```
E:\ensemblemay
├─ assets\        원본 사진·영상 보관함 (사이트가 쓰는 파일 아님)
├─ web\           사이트 소스 (Next.js)
├─ README.md
└─ netlify.toml   ← 이번에 추가
```

뿐이라, 뿌리를 통째로 올리면 Netlify 는 첫 화면을 못 찾고
"Page not found" 만 띄웁니다. 배포가 실패한 게 아니라
**엉뚱한 폴더를 올린 것**입니다.

진짜 사이트는 빌드해야 나옵니다. 빌드 결과는 여기입니다:

```
E:\ensemblemay\web\out\
├─ index.html     ← 첫 화면. Netlify 가 찾는 파일
├─ about\index.html
├─ concert\index.html
├─ gallery\index.html
├─ contact\index.html
├─ _next\         자바스크립트·CSS
└─ assets\        사이트가 실제로 쓰는 사진·영상
```

---

## 방법 1 — GitHub 연결 (권장)

한 번만 해 두면 이후로는 `git push` 만 하면 알아서 배포됩니다.

1. Netlify → **Add new site → Import an existing project**
2. GitHub 의 `qorrbfl/ensemblemay` 선택
3. 빌드 설정은 **묻는 대로 두면 됩니다.** 뿌리의 `netlify.toml` 을
   Netlify 가 읽어서 아래를 자동으로 적용합니다.

   | 항목 | 값 |
   |---|---|
   | Base directory | `web` |
   | Build command | `npm run build` |
   | Publish directory | `out` (base 기준 → `web/out`) |
   | Node version | 22 |

4. Deploy 누르면 끝.

---

## 방법 2 — 폴더 끌어다 놓기 (Netlify Drop)

GitHub 없이 그때그때 올리는 방식입니다. **매번 빌드를 먼저 돌려야 합니다.**

1. 빌드 (PowerShell):

   ```powershell
   cd E:\ensemblemay\web
   npm run build
   ```

2. https://app.netlify.com/drop 에 접속

3. **`E:\ensemblemay\web\out` 폴더를** 끌어다 놓습니다.
   `E:\ensemblemay` 가 아닙니다. `out` 폴더 하나만입니다. (약 21MB)

> 빌드를 건너뛰면 **지난번 빌드한 옛날 화면**이 그대로 올라갑니다.
> `out` 폴더는 자동으로 갱신되지 않습니다.

---

## 자주 겪는 것

**올렸는데 첫 화면이 "Page not found"**
→ `out` 이 아니라 뿌리 폴더를 올렸습니다. 3번을 다시 하세요.

**바꾼 내용이 안 보인다**
→ `npm run build` 를 안 돌렸습니다. 방법 2는 빌드가 필수입니다.

**주소창에 /about/ 직접 쳤더니 404**
→ 정상 동작합니다. `next.config.ts` 의 `trailingSlash: true` 덕분에
   페이지마다 `about\index.html` 이 만들어져 있어 새로고침해도 뜹니다.

**`web/out` 이 git 에 안 올라간다**
→ 맞습니다. `web/.gitignore` 가 `/out/` 을 무시합니다. 빌드 결과물은
   저장소에 넣지 않는 게 정석입니다 — 방법 1을 쓰면 Netlify 가
   서버에서 직접 빌드합니다.
