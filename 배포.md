# 배포 방법 — 앙상블 메이

**Cloudflare Pages + GitHub 연동.** `main` 에 push 하면 자동으로 빌드·배포됩니다.
평소에는 따로 할 일이 없습니다.

| | |
|---|---|
| 주소 | https://ensemblemay.com |
| 미리보기 주소 | https://02-aidenahn-project-04-ensemblemay.pages.dev |
| 저장소 | `CIT-Code-Academy-gyuribaik/02-aidenahn-project-04-EnsembleMAY` |
| Pages 프로젝트 | `02-aidenahn-project-04-ensemblemay` |

---

## 평소 배포

```powershell
git add -A
git commit -m "내용"
git push origin main
```

끝입니다. Cloudflare 가 저장소를 받아 서버에서 직접 빌드합니다 (3~5분).
`main` 이 아닌 브랜치에 push 하면 미리보기 주소만 따로 생기고,
`ensemblemay.com` 은 그대로 있습니다.

빌드 로그는 Cloudflare 대시보드 → **Workers & Pages → 프로젝트 → Deployments** 에 있습니다.

---

## 빌드 설정 (이미 되어 있음)

프로젝트를 새로 만들 일이 있을 때만 씁니다.

| 항목 | 값 |
|---|---|
| Framework preset | **None** |
| Root directory | `web` |
| Build command | `npm run build` |
| Build output directory | `out` |
| Production branch | `main` |
| 환경변수 | `NODE_VERSION` = `22` |

**Framework preset 을 Next.js 로 고르면 안 됩니다.** 이 사이트는
`next.config.ts` 의 `output: "export"` 로 서버가 필요 없는 정적 HTML 을 뽑습니다.
Next.js 프리셋은 서버 런타임을 얹으려 해서 빌드가 깨집니다.

**Root directory 는 반드시 `web`** 입니다. 저장소 뿌리에는 `package.json` 이
없습니다. 비워 두면 첫 빌드가 바로 실패합니다.

---

## 폴더 구조

```
E:\ensemblemay
├─ assets\        원본 사진·영상 보관함 (git 에 올리지 않음, 사이트가 쓰지 않음)
├─ web\           사이트 소스 — 여기가 Root directory
│  ├─ public\assets\   사이트가 실제로 쓰는 사진 (git 에 있음)
│  ├─ content\*.json   공연·단원·사진 목록
│  └─ out\             빌드 결과 (git 에 올리지 않음)
├─ README.md
└─ 배포.md
```

`web/out` 은 `web/.gitignore` 가 무시합니다. 빌드 결과물은 저장소에 넣지 않고,
Cloudflare 가 서버에서 직접 만듭니다.

---

## 로컬에서 확인

```powershell
cd E:\ensemblemay\web
npm run dev        # http://localhost:3000
```

배포될 결과와 똑같은 것을 보려면:

```powershell
npm run build      # web\out 에 생성
```

---

## 도메인

`ensemblemay.com` 은 Cloudflare Pages 의 **Custom domains** 에 연결돼 있고,
DNS 레코드와 인증서는 Cloudflare 가 자동으로 관리합니다. 손댈 것이 없습니다.

주소를 바꿀 일이 생기면 `web/lib/seo.ts` 의 `SITE_URL` 을 고쳐야 합니다.
canonical, `robots.txt`, `sitemap.xml`, 구조화 데이터가 전부 이 값을 씁니다.

---

## 자주 겪는 것

**빌드 실패 — `package.json` 을 못 찾음**
→ Root directory 가 `web` 이 아닙니다.

**빌드 실패 — Node 버전**
→ 환경변수 `NODE_VERSION=22` 가 빠졌습니다.

**바꾼 내용이 안 보인다**
→ push 했는지, Deployments 에서 최신 빌드가 성공했는지 보세요.
   브라우저 강력 새로고침(Ctrl+Shift+R)도 해 보세요.

**주소창에 /about/ 직접 쳤더니 404**
→ 정상 동작합니다. `trailingSlash: true` 라 `about/index.html` 이
   만들어져 있어 새로고침해도 뜹니다.
